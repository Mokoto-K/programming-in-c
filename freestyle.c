#include <stdio.h>

// I dont know if i can use structs... i wanna though, kinda the whole reason
// for doing this.
struct listelement {
    char *todo;
};

void addtodo();
void deletetodo();
void edittodo();

int main() {
    char **todos;

    // While no EOF mans.
        // For todo in todos print.
        // Wait for command.
    return 0;
}
